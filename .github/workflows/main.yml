name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Configure Git
        run: |
          git config --local user.email "ashishd52280@gmail.com"
          git config --local user.name "Ashish"

      - name: Auto commit (if changes made by workflow)
        run: |
          # Placeholder for workflow-driven changes
          # git add .
          # git commit -m "Automated changes from workflow" || echo "No changes to commit"
          # git push
